# Expose only the health endpoint for the platform's health check.
management.endpoints.web.exposure.include=health

# Finish in-flight requests on SIGTERM instead of dropping them — this is the
# app contract for zero-downtime deploys (see fast-infra README).
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=25s
