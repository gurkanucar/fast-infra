# Runtime state on the VPS — never commit
infra/.env
infra/.gh_token
infra/backups/
apps/*/
!apps/.gitkeep

# Local build artifacts
cli/platform
platform
*.exe

# Local tooling / editor state
.claude/
